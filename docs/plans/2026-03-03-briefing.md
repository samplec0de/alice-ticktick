# Briefing Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Реализовать утренний и вечерний брифинг (FR-20) через API v1 — один запрос, два типа отчётов.

**Architecture:** Два новых обработчика `handle_morning_briefing` / `handle_evening_briefing` в `handlers.py`. Один вызов `_gather_all_tasks` внутри каждого, затем фильтрация на стороне сервера. Два чистых вспомогательных метода для сборки текста (тестируются без моков). Утро = задачи на сегодня + просроченные; вечер = задачи на завтра.

**Tech Stack:** Python 3.12+, aliceio, pytest-asyncio. Паттерны из `handle_overdue_tasks` / `handle_list_tasks`.

---

### Task 1: Константы — intent IDs и строки ответов

**Files:**
- Modify: `alice_ticktick/dialogs/intents.py`
- Modify: `alice_ticktick/dialogs/responses.py`

**Step 1: Добавить intent-константы в `intents.py`**

В конец блока констант (после `CREATE_PROJECT = "create_project"`) добавить:

```python
MORNING_BRIEFING = "morning_briefing"
EVENING_BRIEFING = "evening_briefing"
```

Также добавить их в `ALL_INTENTS`:

```python
ALL_INTENTS = frozenset(
    {
        # ... existing ...
        MORNING_BRIEFING,
        EVENING_BRIEFING,
    }
)
```

**Step 2: Добавить строки в `responses.py`**

В конец файла перед строкой `UNKNOWN`:

```python
# Briefing
MORNING_BRIEFING_NO_TASKS = "Доброе утро! На сегодня задач нет. Можно начать день спокойно."
MORNING_BRIEFING_NO_TASKS_OVERDUE = (
    "Доброе утро! На сегодня задач нет, но есть {overdue_count} просроченных."
)
MORNING_BRIEFING_TASKS = "Доброе утро! На сегодня {count}:\n{tasks}"
MORNING_BRIEFING_TASKS_OVERDUE = (
    "Доброе утро! На сегодня {count}:\n{tasks}\nПросрочено: {overdue_count}."
)
EVENING_BRIEFING_NO_TASKS = "Итоги дня! На завтра задач нет. Хорошего вечера!"
EVENING_BRIEFING_TASKS = "Итоги дня! На завтра {count}:\n{tasks}\nХорошего вечера!"
```

**Step 3: Запустить тесты (убедиться, что всё ещё зелёные)**

```bash
uv run pytest -v -x
```

Expected: все тесты проходят.

**Step 4: Commit**

```bash
git add alice_ticktick/dialogs/intents.py alice_ticktick/dialogs/responses.py
git commit -m "feat: константы для брифинга — intent IDs и строки ответов"
```

---

### Task 2: Чистые вспомогательные функции + тесты

**Files:**
- Modify: `alice_ticktick/dialogs/handlers.py`
- Create: `tests/test_handlers_briefing.py`

**Step 1: Написать тесты (failing)**

Создать `tests/test_handlers_briefing.py`:

```python
"""Tests for morning/evening briefing handlers."""

from __future__ import annotations

import datetime
from unittest.mock import AsyncMock, MagicMock
from zoneinfo import ZoneInfo

import pytest

from alice_ticktick.dialogs import responses as txt
from alice_ticktick.dialogs.handlers import (
    _build_evening_briefing_text,
    _build_morning_briefing_text,
    _reset_project_cache,
    handle_evening_briefing,
    handle_morning_briefing,
)
from alice_ticktick.ticktick.models import Project, Task


@pytest.fixture(autouse=True)
def _clear_cache() -> None:
    _reset_project_cache()


def _make_task(
    *,
    task_id: str = "t1",
    title: str = "Test",
    due_date: datetime.datetime | None = None,
    status: int = 0,
) -> Task:
    return Task(
        id=task_id,
        title=title,
        projectId="proj-1",
        priority=0,
        status=status,
        dueDate=due_date,
    )


def _make_message(*, access_token: str | None = "token") -> MagicMock:
    msg = MagicMock()
    msg.command = ""
    msg.nlu = None
    if access_token is not None:
        msg.user = MagicMock()
        msg.user.access_token = access_token
    else:
        msg.user = None
    return msg


def _make_mock_client(tasks: list[Task]) -> MagicMock:
    client = AsyncMock()
    client.get_projects = AsyncMock(return_value=[Project(id="proj-1", name="Inbox")])
    client.get_tasks = AsyncMock(return_value=tasks)
    client.get_inbox_tasks = AsyncMock(return_value=[])
    factory = MagicMock()
    factory.return_value.__aenter__ = AsyncMock(return_value=client)
    factory.return_value.__aexit__ = AsyncMock(return_value=None)
    return factory


UTC = ZoneInfo("UTC")


# --- _build_morning_briefing_text ---


def test_morning_briefing_text_no_tasks_no_overdue() -> None:
    result = _build_morning_briefing_text(today_tasks=[], overdue_tasks=[], tz=UTC)
    assert result == txt.MORNING_BRIEFING_NO_TASKS


def test_morning_briefing_text_no_tasks_with_overdue() -> None:
    overdue = [_make_task(title="A"), _make_task(task_id="t2", title="B")]
    result = _build_morning_briefing_text(today_tasks=[], overdue_tasks=overdue, tz=UTC)
    assert "2" in result
    assert "просроч" in result.lower()


def test_morning_briefing_text_tasks_no_overdue() -> None:
    today = [_make_task(title="Задача 1")]
    result = _build_morning_briefing_text(today_tasks=today, overdue_tasks=[], tz=UTC)
    assert "Задача 1" in result
    assert "просроч" not in result.lower()


def test_morning_briefing_text_tasks_with_overdue() -> None:
    today = [_make_task(title="Задача 1")]
    overdue = [_make_task(task_id="t2", title="Старая задача")]
    result = _build_morning_briefing_text(today_tasks=today, overdue_tasks=overdue, tz=UTC)
    assert "Задача 1" in result
    assert "1" in result  # overdue count
    assert "просроч" in result.lower()


def test_morning_briefing_text_caps_at_five() -> None:
    today = [_make_task(task_id=str(i), title=f"T{i}") for i in range(10)]
    result = _build_morning_briefing_text(today_tasks=today, overdue_tasks=[], tz=UTC)
    assert "T6" not in result  # только первые 5


# --- _build_evening_briefing_text ---


def test_evening_briefing_text_no_tasks() -> None:
    result = _build_evening_briefing_text(tomorrow_tasks=[], tz=UTC)
    assert result == txt.EVENING_BRIEFING_NO_TASKS


def test_evening_briefing_text_with_tasks() -> None:
    tomorrow = [_make_task(title="Завтрашняя задача")]
    result = _build_evening_briefing_text(tomorrow_tasks=tomorrow, tz=UTC)
    assert "Завтрашняя задача" in result
    assert "завтра" in result.lower()


def test_evening_briefing_text_caps_at_five() -> None:
    tomorrow = [_make_task(task_id=str(i), title=f"T{i}") for i in range(10)]
    result = _build_evening_briefing_text(tomorrow_tasks=tomorrow, tz=UTC)
    assert "T6" not in result
```

**Step 2: Запустить тест (должен упасть)**

```bash
uv run pytest tests/test_handlers_briefing.py -v 2>&1 | head -30
```

Expected: `ImportError` — функции ещё не существуют.

**Step 3: Реализовать вспомогательные функции в `handlers.py`**

Добавить после функции `_truncate_response` (≈строка 177):

```python
def _build_morning_briefing_text(
    today_tasks: list[Task],
    overdue_tasks: list[Task],
    tz: ZoneInfo,
) -> str:
    """Build morning briefing text from today's and overdue tasks."""
    overdue_count = len(overdue_tasks)

    if not today_tasks:
        if overdue_count:
            return txt.MORNING_BRIEFING_NO_TASKS_OVERDUE.format(
                overdue_count=txt.pluralize_tasks(overdue_count)
            )
        return txt.MORNING_BRIEFING_NO_TASKS

    count_str = txt.pluralize_tasks(len(today_tasks))
    lines = [_format_task_line(i + 1, t) for i, t in enumerate(today_tasks[:5])]
    task_list = "\n".join(lines)

    if overdue_count:
        return txt.MORNING_BRIEFING_TASKS_OVERDUE.format(
            count=count_str,
            tasks=task_list,
            overdue_count=txt.pluralize_tasks(overdue_count),
        )
    return txt.MORNING_BRIEFING_TASKS.format(count=count_str, tasks=task_list)


def _build_evening_briefing_text(
    tomorrow_tasks: list[Task],
    tz: ZoneInfo,
) -> str:
    """Build evening briefing text from tomorrow's tasks."""
    if not tomorrow_tasks:
        return txt.EVENING_BRIEFING_NO_TASKS

    count_str = txt.pluralize_tasks(len(tomorrow_tasks))
    lines = [_format_task_line(i + 1, t) for i, t in enumerate(tomorrow_tasks[:5])]
    task_list = "\n".join(lines)
    return txt.EVENING_BRIEFING_TASKS.format(count=count_str, tasks=task_list)
```

**Step 4: Запустить тесты вспомогательных функций**

```bash
uv run pytest tests/test_handlers_briefing.py -v -k "morning_briefing_text or evening_briefing_text"
```

Expected: 8 тестов — PASSED.

**Step 5: Commit**

```bash
git add alice_ticktick/dialogs/handlers.py tests/test_handlers_briefing.py
git commit -m "feat: вспомогательные функции _build_morning/evening_briefing_text + тесты"
```

---

### Task 3: Обработчик handle_morning_briefing + тесты

**Files:**
- Modify: `alice_ticktick/dialogs/handlers.py`
- Modify: `tests/test_handlers_briefing.py`

**Step 1: Написать тесты обработчика (failing)**

Добавить в `tests/test_handlers_briefing.py`:

```python
# --- handle_morning_briefing ---


async def test_morning_briefing_auth_required() -> None:
    msg = _make_message(access_token=None)
    response = await handle_morning_briefing(msg)
    assert response.text == txt.AUTH_REQUIRED_NO_LINKING


async def test_morning_briefing_no_tasks() -> None:
    msg = _make_message()
    factory = _make_mock_client([])
    response = await handle_morning_briefing(msg, ticktick_client_factory=factory)
    assert "Доброе утро" in response.text
    assert "задач нет" in response.text


async def test_morning_briefing_with_today_tasks() -> None:
    tz = ZoneInfo("UTC")
    today = datetime.datetime.now(tz=tz).replace(hour=12, microsecond=0)
    tasks = [_make_task(title="Задача сегодня", due_date=today)]
    msg = _make_message()
    factory = _make_mock_client(tasks)
    response = await handle_morning_briefing(msg, ticktick_client_factory=factory)
    assert "Задача сегодня" in response.text
    assert "Доброе утро" in response.text


async def test_morning_briefing_with_overdue() -> None:
    tz = ZoneInfo("UTC")
    yesterday = datetime.datetime.now(tz=tz) - datetime.timedelta(days=1)
    yesterday = yesterday.replace(hour=12, microsecond=0)
    tasks = [_make_task(title="Просроченная", due_date=yesterday)]
    msg = _make_message()
    factory = _make_mock_client(tasks)
    response = await handle_morning_briefing(msg, ticktick_client_factory=factory)
    assert "просроч" in response.text.lower()
    assert "задач нет" in response.text  # today has no tasks


async def test_morning_briefing_api_error() -> None:
    msg = _make_message()
    factory = MagicMock()
    factory.return_value.__aenter__ = AsyncMock(side_effect=Exception("API down"))
    factory.return_value.__aexit__ = AsyncMock(return_value=None)
    response = await handle_morning_briefing(msg, ticktick_client_factory=factory)
    assert response.text == txt.API_ERROR
```

**Step 2: Запустить (должен упасть)**

```bash
uv run pytest tests/test_handlers_briefing.py -v -k "morning_briefing" 2>&1 | head -20
```

Expected: `ImportError` — `handle_morning_briefing` не существует.

**Step 3: Реализовать `handle_morning_briefing` в `handlers.py`**

Добавить перед `handle_unknown` (в конце файла):

```python
async def handle_morning_briefing(
    message: Message,
    ticktick_client_factory: type[TickTickClient] | None = None,
    event_update: Update | None = None,
) -> Response:
    """Handle morning_briefing intent — today's tasks + overdue count."""
    access_token = _get_access_token(message)
    if access_token is None:
        return _auth_required_response(event_update)

    user_tz = _get_user_tz(event_update)
    factory = ticktick_client_factory or TickTickClient
    today = datetime.datetime.now(tz=user_tz).date()
    tomorrow = today + datetime.timedelta(days=1)

    try:
        async with factory(access_token) as client:
            all_tasks = await _gather_all_tasks(client)
    except Exception:
        logger.exception("Failed to get tasks for morning briefing")
        return Response(text=txt.API_ERROR)

    active = [t for t in all_tasks if t.status == 0]
    today_tasks = [
        t for t in active if t.due_date is not None and _to_user_date(t.due_date, user_tz) == today
    ]
    overdue_tasks = [
        t for t in active if t.due_date is not None and _to_user_date(t.due_date, user_tz) < today
    ]

    text = _build_morning_briefing_text(today_tasks, overdue_tasks, user_tz)
    return Response(text=_truncate_response(text))
```

**Step 4: Запустить тесты**

```bash
uv run pytest tests/test_handlers_briefing.py -v -k "morning_briefing"
```

Expected: 6 тестов — PASSED.

**Step 5: Commit**

```bash
git add alice_ticktick/dialogs/handlers.py tests/test_handlers_briefing.py
git commit -m "feat: handle_morning_briefing — утренний брифинг + тесты"
```

---

### Task 4: Обработчик handle_evening_briefing + тесты

**Files:**
- Modify: `alice_ticktick/dialogs/handlers.py`
- Modify: `tests/test_handlers_briefing.py`

**Step 1: Написать тесты обработчика (failing)**

Добавить в `tests/test_handlers_briefing.py`:

```python
# --- handle_evening_briefing ---


async def test_evening_briefing_auth_required() -> None:
    msg = _make_message(access_token=None)
    response = await handle_evening_briefing(msg)
    assert response.text == txt.AUTH_REQUIRED_NO_LINKING


async def test_evening_briefing_no_tasks() -> None:
    msg = _make_message()
    factory = _make_mock_client([])
    response = await handle_evening_briefing(msg, ticktick_client_factory=factory)
    assert "Итоги дня" in response.text
    assert "задач нет" in response.text


async def test_evening_briefing_with_tomorrow_tasks() -> None:
    tz = ZoneInfo("UTC")
    tomorrow = (datetime.datetime.now(tz=tz) + datetime.timedelta(days=1)).replace(
        hour=12, microsecond=0
    )
    tasks = [_make_task(title="Завтрашняя задача", due_date=tomorrow)]
    msg = _make_message()
    factory = _make_mock_client(tasks)
    response = await handle_evening_briefing(msg, ticktick_client_factory=factory)
    assert "Завтрашняя задача" in response.text
    assert "Итоги дня" in response.text


async def test_evening_briefing_ignores_today_tasks() -> None:
    tz = ZoneInfo("UTC")
    today = datetime.datetime.now(tz=tz).replace(hour=12, microsecond=0)
    tasks = [_make_task(title="Сегодняшняя задача", due_date=today)]
    msg = _make_message()
    factory = _make_mock_client(tasks)
    response = await handle_evening_briefing(msg, ticktick_client_factory=factory)
    assert "задач нет" in response.text  # tomorrow is empty


async def test_evening_briefing_api_error() -> None:
    msg = _make_message()
    factory = MagicMock()
    factory.return_value.__aenter__ = AsyncMock(side_effect=Exception("API down"))
    factory.return_value.__aexit__ = AsyncMock(return_value=None)
    response = await handle_evening_briefing(msg, ticktick_client_factory=factory)
    assert response.text == txt.API_ERROR
```

**Step 2: Запустить (должен упасть)**

```bash
uv run pytest tests/test_handlers_briefing.py -v -k "evening_briefing" 2>&1 | head -20
```

Expected: `ImportError`.

**Step 3: Реализовать `handle_evening_briefing` в `handlers.py`**

Добавить сразу после `handle_morning_briefing`:

```python
async def handle_evening_briefing(
    message: Message,
    ticktick_client_factory: type[TickTickClient] | None = None,
    event_update: Update | None = None,
) -> Response:
    """Handle evening_briefing intent — tomorrow's tasks preview."""
    access_token = _get_access_token(message)
    if access_token is None:
        return _auth_required_response(event_update)

    user_tz = _get_user_tz(event_update)
    factory = ticktick_client_factory or TickTickClient
    tomorrow = (datetime.datetime.now(tz=user_tz) + datetime.timedelta(days=1)).date()

    try:
        async with factory(access_token) as client:
            all_tasks = await _gather_all_tasks(client)
    except Exception:
        logger.exception("Failed to get tasks for evening briefing")
        return Response(text=txt.API_ERROR)

    tomorrow_tasks = [
        t
        for t in all_tasks
        if t.due_date is not None
        and _to_user_date(t.due_date, user_tz) == tomorrow
        and t.status == 0
    ]

    text = _build_evening_briefing_text(tomorrow_tasks, user_tz)
    return Response(text=_truncate_response(text))
```

**Step 4: Запустить все тесты брифинга**

```bash
uv run pytest tests/test_handlers_briefing.py -v
```

Expected: все тесты — PASSED.

**Step 5: Запустить полный suite**

```bash
uv run pytest -v
```

Expected: все тесты — PASSED.

**Step 6: Commit**

```bash
git add alice_ticktick/dialogs/handlers.py tests/test_handlers_briefing.py
git commit -m "feat: handle_evening_briefing — вечерний брифинг + тесты"
```

---

### Task 5: Регистрация роутов в router.py

**Files:**
- Modify: `alice_ticktick/dialogs/router.py`

**Step 1: Добавить импорты в `router.py`**

В блок импортов из `handlers`:
```python
from alice_ticktick.dialogs.handlers import (
    # ... existing ...
    handle_evening_briefing,
    handle_morning_briefing,
)
```

В блок импортов из `intents`:
```python
from alice_ticktick.dialogs.intents import (
    # ... existing ...
    EVENING_BRIEFING,
    MORNING_BRIEFING,
)
```

**Step 2: Зарегистрировать роуты**

Добавить после блока `on_help` (≈строка 104), перед ADD_SUBTASK-роутом:

```python
@router.message(IntentFilter(MORNING_BRIEFING))
async def on_morning_briefing(message: Message, event_update: Update) -> Response:
    """Handle morning_briefing intent."""
    return await handle_morning_briefing(message, event_update=event_update)


@router.message(IntentFilter(EVENING_BRIEFING))
async def on_evening_briefing(message: Message, event_update: Update) -> Response:
    """Handle evening_briefing intent."""
    return await handle_evening_briefing(message, event_update=event_update)
```

**Step 3: Запустить тесты**

```bash
uv run pytest -v
```

Expected: все тесты — PASSED.

**Step 4: Линтинг и типы**

```bash
uv run ruff check . && uv run ruff format . && uv run mypy alice_ticktick/
```

Expected: без ошибок.

**Step 5: Commit**

```bash
git add alice_ticktick/dialogs/router.py
git commit -m "feat: роуты morning_briefing и evening_briefing в router"
```

---

### Task 6: Обновить HELP и документацию

**Files:**
- Modify: `alice_ticktick/dialogs/responses.py`
- Modify: `docs/SETUP.md`

**Step 1: Обновить HELP**

В `responses.py` добавить пункт в строку `HELP` (перед закрывающей кавычкой):

```python
HELP = (
    # ... existing lines ...
    "- Брифинг: «доброе утро» или «подведи итоги дня»\n"
    ...
)
```

**Step 2: Добавить NLU-грамматики в SETUP.md**

В раздел `## 4. Настройка интентов (NLU)` добавить:

```markdown
### 4.17. morning_briefing — Утренний брифинг

**Название:** Утренний брифинг
**ID:** `morning_briefing`

**Грамматика:**
```
root:
    %lemma
    (доброе|добрый) (утро|день)
    (утренний|утренняя) (брифинг|сводка|план)
    расскажи (мне)? (план|сводку) (на день|на сегодня)
```

**Положительные тесты:**
```
доброе утро
добрый день
утренний брифинг
расскажи план на день
```

**Отрицательные тесты:**
```
покажи задачи на сегодня
добрый вечер
подведи итоги
```

### 4.18. evening_briefing — Вечерний брифинг

**Название:** Вечерний брифинг
**ID:** `evening_briefing`

**Грамматика:**
```
root:
    %lemma
    (подведи|подведём) (итоги|итог) (дня|за день)?
    (вечерний|вечерняя) (брифинг|сводка)
    итоги (дня|за день)
    что (я)? (сделал|выполнил) (сегодня|за день)
```

**Положительные тесты:**
```
подведи итоги дня
вечерний брифинг
итоги за день
что я сделал сегодня
подведём итоги
```

**Отрицательные тесты:**
```
покажи задачи на завтра
доброе утро
что на завтра
```
```

**Step 3: Обновить таблицу прогресса в SETUP.md**

Изменить строку Phase 4 с `⬜ В очереди` на статус в работе (когда PR будет готов — `✅ Готово`).

**Step 4: Запустить тесты**

```bash
uv run pytest -v
```

**Step 5: Commit**

```bash
git add alice_ticktick/dialogs/responses.py docs/SETUP.md
git commit -m "docs: NLU-грамматики и обновление HELP для брифинга"
```

---

### Task 7: Настроить интенты в Яндекс Диалогах (ручной шаг)

**Это ручной шаг, выполняется через браузер.**

1. Открыть: https://dialogs.yandex.ru/developer/skills/d3f073db-dece-42b8-9447-87511df30c83/draft/settings/intents
2. Создать интент `morning_briefing` с грамматикой из Task 6
3. Создать интент `evening_briefing` с грамматикой из Task 6
4. Нажать «Сохранить» и «Протестировать»
5. Проверить: Точность и Полнота — 100% для каждого интента
6. Опубликовать черновик

---

### Task 8: Финал — PR

```bash
git log --oneline -6
```

Убедиться, что все коммиты на месте, затем:

```bash
uv run pytest -v
uv run ruff check .
uv run mypy alice_ticktick/
```

Создать PR с описанием на русском. Использовать skill `commit-commands:commit-push-pr`.
